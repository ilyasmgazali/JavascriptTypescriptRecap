# without ternary
=================
var age = 24

if (age >= 21) {
  console.log("Beer")
} else {
  console.log("Juice")
}

# ternary example
=================

var age = 24;
// (if age is over 21) 'then' print Beer 'or else' Juice
age >= 21 ? "Beer" : "Juice"; //------------- left side true, right side false

# further reading
=================
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Conditional_Operator